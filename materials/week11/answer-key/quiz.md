# Answer Key

1. **B** — a cache is a small, very fast memory placed close to the processor
2. **C** — a cache hit means the processor finds the data already in the cache
3. **A** — locality (temporal and spatial) is why caching usually works
4. **B** — 45 / 50 = 90%
5. **C** — AMAT = 2 + (0.10 × 80) = 2 + 8 = 10 ns
6. **C** — a compulsory miss happens the very first time data is requested
7. **A** — fast memory (cache) needs more parts per bit, so it costs more money, power, and chip space to build large
8. **Model answer:** "A bigger cache usually reaches a higher hit rate, so the processor needs fewer slow trips to main memory. That lowers AMAT, which means less waiting on every memory request, adding up to a faster finish overall."
