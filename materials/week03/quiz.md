# Week 3 Self-Check Quiz (Ungraded)

Computer Architecture (503872-004). This quiz is **ungraded** — it is
only to help you check what stuck. About 10 minutes. Do not look back at
the slides while you answer.

---

**1.** What is a "bit"?

A. A group of 8 switches
B. One single 0 or 1, the smallest piece of stored information
C. A shorter way to write hexadecimal
D. The part of a processor that does math

**2.** Convert the decimal number **14** to binary.

A. `1100`
B. `1110`
C. `1111`
D. `0111`

**3.** Why do engineers use hexadecimal at all?

A. It is a completely different kind of number from binary
B. It is a short, easy-to-read way to write the same bits
C. Computers cannot understand binary directly
D. Hexadecimal is only used for text, never numbers

**4.** What trick does two's complement use to write a negative number?

A. Just change the first digit to a minus sign
B. Invert every bit, then add 1
C. Write the number backwards
D. Multiply the number by -1 in decimal

**5.** What is "overflow"?

A. When a computer runs out of electricity
B. When a result needs more bits than are available, and wraps to a wrong number
C. When a number is written in the wrong base
D. When two characters share the same ASCII number

**6.** What does ASCII do?

A. Converts binary to hexadecimal
B. Assigns every text character an agreed number
C. Fixes overflow automatically
D. Measures a processor's clock speed

**7.** The byte `11111110` is stored in memory. What is it, read as
**unsigned**? What is it, read as **signed two's complement**?

`_____________________________________________________________`

**8. (Short answer)** In 1-2 sentences, explain why the exact same byte
can represent two different decimal numbers. Use at least one key word
from this week (bit, sign bit, two's complement, or overflow).

`_____________________________________________________________`
`_____________________________________________________________`
